Pasos para crear una API

Comandos:

1. npm init -y : inicializa el proyecto
2. npm install typescript ts-node @types/node --save-dev
3. npm install express @types/express body-parser dotenv mysql2
4. npm install bcryptjs jsonwebtoken @types/bcryptjs @types/jsonwebtoken
5. npx ts --init
6. 